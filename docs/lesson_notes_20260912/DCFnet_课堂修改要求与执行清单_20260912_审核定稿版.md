# DCFnet 课堂修改要求与执行清单（2026-09-12 审核定稿版）

本文件是 **2026-09-12 课堂修改要求的唯一执行版**：把原清单（[DCFnet_课堂修改要求与执行清单_20260912.md](D:/STUDY/My_github/sci_project/docs/lesson_notes_20260912/DCFnet_课堂修改要求与执行清单_20260912.md)）与独立复核结论（[DCFnet_课堂要求独立复核与遗漏补充_20260912.md](D:/STUDY/My_github/sci_project/docs/lesson_notes_20260912/DCFnet_课堂要求独立复核与遗漏补充_20260912.md)）合并，已把修正直接写进对应条目，不需要再对照两份文件。

- **标注规则**：条目后带 ★ 表示本次复核新增或修正；不带 ★ 的条目沿用原清单，内容未变。
- **证据来源**：老师 Word 逐 run 提取的红字（73 处）/绿字（28 处）批注、与 0906 稿的文字 diff、完整课堂逐字稿（396 段 / 3.4 万字 / `00:06:36—01:40:25`，见 [20260912_课堂逐字稿_腾讯会议自动转写.md](D:/STUDY/My_github/sci_project/docs/lesson_notes_20260912/20260912_课堂逐字稿_腾讯会议自动转写.md)）、你的课堂笔记、论文现稿、项目审计与交接文档。
- **状态**：本文件是要求清单，未修改论文、PPT、图源，也未运行新实验。

## 0 本次复核新增与修正项（速览）

| 类型 | 条目 | 内容 |
|---|---|---|
| 修正 | P01 ★ | 项目/仓库名不得含 "SCI"，改成与论文名相关的命名，并同步摘要/Data and Code/README 里的地址（逐字稿 00:09:28 核实） |
| 新增 | P07 ★ | 若第三创新点采用主体/前景模块，要在 contribution 处展开描述，并配一张说明设计动机（core idea）的图（00:40:00—00:41:33） |
| 修正 | P05 ★ | 除核查 "first" 外，**同时要求写出明确的空白与本文定位**："existing works focus on … without considering …；this work focuses on …"，措辞可以强但要自洽（00:49:20—00:51:03） |
| 修正 | F01 ★ | 补"**用两种背景色区分两个任务**"这条明确动作（01:06:17—01:07:06） |
| 修正 | F05 ★ | 补"**用虚线把图内公式连到所属模块**"，虚线要弱于主流程实线（01:08:42—01:12:13） |
| 修正 | N01 ★ | 把两条字号口径分开：符号统一时可放松（01:04:00）；**图内承载信息的文字必须大于等于正文**（01:21:16，原清单漏记） |
| 修正 | E03 ★ | 老师明确"**DINO-only 不能算消融**"，消融只放"去模块/去分支"类真消融（01:33:12、01:35:16） |
| 修正 | E06 ★ | 补"**同一数据集合并单元格/行**"，老师已用颜色在 Word 里标出（01:26:35—01:27:06，对应主结果表与类别表的 Dataset 列标红） |
| 修正 | E01 ★ | 补细节：≈10 种方法 + GT，**本文方法与 GT 相邻**，样品 ≥3（01:29:45—01:32:33） |
| 修正 | E02 ★ | 补细节：**红色不填充线框**标缺陷 + 放大窗；老师说现有例子不够，**要 5 个**（01:33:12—01:35:16） |
| 新增 | 阶段 2/5 ★ | 建"正文图号 ↔ 图源 ↔ PPT 页 ↔ 脚本 ↔ 版本日期"清单（正文引用 Fig. 4–6，最新图包只有 5 张方法图） |
| 新增 | 待确认 9 ★ | 贡献处"设计动机图"由谁画；老师答应发的样例图需主动跟催 |

---

# 一 论文定位与贡献重构

## P01 核对项目名称与全文定位 ★

- **要求**：项目/仓库名不得含 "SCI"，要改成与论文名相关的学术命名；同时核对题目、DCFnet 全称、摘要、引言末段、方法概览和结论是否表达同一研究问题。
- **出处**：逐字稿 00:09:28（"那个项目名字不能写 S ci project……尽量和你的 paper 的名字相关，比如说就叫做 DCFnet project，就比较学术正规一点"）；摘要代码地址被老师标红。
- **动作**：① 定正式项目名（去 SCI）；② 改仓库名或建立新地址；③ 同步摘要末、Data and Code Availability、`README.md`、`docs/submission_reproducibility_20260826/README.md`、复现包说明中的 URL；④ 改名后实测链接可访问；⑤ 若必须保留旧仓库名，需在仓库内做跳转说明。
- **现状**：论文标题已是 DCFnet，但地址仍是 `github.com/USEU117/sci_project`；现有实现是冻结的双视觉编码器方法，若增加多分支边界分析，需说明它是实验分析范围，不能让标题或主图暗示已实现视觉与文本的多分支统一系统。
- **验收**：全文出现的项目名与仓库地址一致、可访问、不含 SCI；今天资料没有给出最终新项目名，不代拟为已定稿名称。

## P02 不把常规实验评估单独包装成核心创新

- **要求**：原第三点以"评估完整流程、报告指标与资源"为主，需要重新组织。实验用于证明方法价值；只有研究问题、实验设计及发现本身确有独特性并经过相关工作核查时，才可谨慎表述为实证贡献。
- **出处**：Word 贡献段、你的笔记、逐字稿 00:10:34—00:12:43（"the contribution 是创新点的意思……评估它并不能作为一个创新点"）。
- **验收**：不能仅以"做了大量实验"占据一个方法创新点。

## P03 贡献要说清模块的价值和与现有方法的区别

- **要求**：解释模块解决什么困难、为何需要这样组合、与已有方案哪里不同、为什么有效。单纯调用已有层、插值、归一化或拼接，不自动成为新的模块创新。
- **出处**：逐字稿 00:12:54—00:15:19、00:36:48；Word 的 `Novel framework`、`XXX Module` 等占位。
- **建议结构**：第一点概括统一框架及适用场景；第二点说明网格对齐与幅值校准解决的问题；第三点仅在独立价值成立时，说明联合描述子匹配或其他真实模块的作用；最后用一句话概括匹配控制下的实证发现。第二、三点能否并列，要由技术差异和消融支撑，不能为了凑三点机械拆开（逐字稿 00:35:47—00:38:26：拆分可以，但每点都要说清"别人不这么设计，我这么设计效果就是好"）。
- **★ 登记候选方向**：老师明确提议参考 ROI / 前景聚焦机制作为创新点（00:21:40—00:26:49），并给出的动机是"背景不参与运算可降低计算成本"。该方向同时见 `docs/FOREGROUND_INNOVATION_ADDENDUM_20260912_CN.md`（E7-F：F0 掩码诊断 + F1 六组对照）与 `docs/AI_HANDOFF_VALIDATION_AND_INNOVATION_20260911_CN.md`（E7-P）；已有真实负结果：PCA 前景硬掩码在 MPDD s0 K2/K4 宏 P-AP 上 DINO-B −0.0254、DINO-S +0.0065。**论文中只能写在局限或未来工作，不得写成已实现模块。**
- **验收**：每一点都能对应到方法小节、图中模块和验证证据；没有重复介绍同一操作，没有未实现的 DYT、ROI 或前景提取策略，也没有 `XXX`、`Module…`、`First-proposed…` 等课堂占位句。

## P04 区分已实现框架与拟开展的多分支研究

- **要求**：在当前设置下先写"观察到的收益与局限"，再规划第三分支等扩展；三分支对比属于实验分析部分，不能进 Overview 说成已完成设计。
- **出处**：逐字稿 00:31:48—00:35:30、00:43:38—00:45:38。
- **动作**：比较需同时报告准确性和代价，允许出现饱和、负迁移或更强对照；有限配置不能证明普遍的"准确率上限"或全局最佳融合方式。
- **验收**：多分支内容出现在实验章节；Overview 与主图不出现当前未实现的分支。

## P05 首创性核查 **+ 明确的空白陈述** ★

- **要求（核查面）**：先比较已有双分支、多尺度、多编码器与融合方法，明确本工作区别；"first work" 是讨论中的写作示例与待验证判断，不是已经成立的事实。补近两年的对照工作，逐条核验任务、训练设置和本地结果。
- **要求（写作面，原清单缺）★**：同时要写出明确的 gap 与本文定位，措辞可以强。逐字稿 00:49:20—00:51:03 给出示范："existing experiments focus on … without any consideration of（某细节）…… this work focuses on 弥补别人不考虑的这个点"；"novel 或 first proposed 敢写吗？你要确定到时候确定能自洽就行"；老师说自己的论文敢写 "it is the first work" 是因为"做了大量的文献调研有支撑"。
- **验收**：每条强势表述都能在相关工作里找到支撑文献并自洽；没有未经验证的 "first" 或上限结论。

## P07 第三创新点的图文配套 ★（新增）

- **要求**：若第三创新点采用主体/前景（ROI）模块，要在贡献处把该模块展开描述，并配一张说明设计动机（core idea / design motivation）的图。
- **出处**：逐字稿 00:40:00—00:41:33（"你能用作为 contribution 的时候，希望在这个位置能够展开来"；"在这里给出一个 figure"）。
- **动作**：先确认该模块是否进入本轮论文；进入则贡献段展开 + 配设计动机图；不进入则只在局限/未来工作提及。
- **验收**：贡献处要么展开且有配图，要么明确不写；不能出现"提及了模块但正文无处解释"的情况。

---

# 二 主图的完整修改要求

## F01 让主图达到图文摘要的表达质量

- **要求**：读者要能迅速看懂任务输入、两个核心过程、关键机制和输出结果。提高有效信息密度，缩小无意义空白，放大文字与实例，统一上下路径的比例，让边框、箭头和颜色分组在论文缩放后仍清晰。
- **出处**：逐字稿 00:53:41—00:55:46、01:17:07—01:19:11、01:21:16；你的笔记。
- **★ 补充动作**：把 `Normalized` 与 `Concatenate` 两个任务**用两种背景色区分**（老师原话："他两个任务用两个背景色""你可以用两个背景色知道它是两层任务"，01:06:17—01:07:06）；上下两部分（A/B）尺寸调成一致（01:05:00—01:05:21）。
- **验收**：不靠添加装饰或堆放公式填空；主图在论文实际栏宽下看得到主要信息。

## F02 输入遵循 Problem statement

- **要求**：支持端展示同一类别的多张正常参考图，以集合或多图排列体现 K 张输入，并对应正文中的类别与样本索引；查询端保持待检测图像的语义。
- **出处**：此前明确修改要求、逐字稿 00:58:02 附近（老师核对 Problem statement 的 R_c、K 定义）。
- **验收**：支持图和查询图不能混淆；多个参考样本不能画成多种缺陷类别。

## F03 清楚区分建库与查询

- **要求（执行建议）**：上路表示正常支持图经过共享冻结编码器、网格对齐、归一化与融合后建立类别记忆库；下路表示查询图经过相同特征变换后读取固定记忆库，计算 patch 异常分数，再恢复为图像尺度的异常图。两条路径用相同模块名称与视觉编码；查询不回写正常库。
- **验收**：读者可沿箭头从每种输入追踪到结果，能辨认哪些参数冻结、哪些对象共享、何时建库、何时检索；主图不额外引入当前没有实现的训练、文本分支、PCA、动态权重或阈值学习过程。

## F04 最后展示轮廓图与对应热力图

- **要求**：老师要求同时展示轮廓及其对应热图，**不是**"热力图加一个图像分数"就算两张。两个结果应来自同一查询样本并排显示，标明各自含义；**实验图（如案例图）也要两种都输出**（逐字稿 01:28:39—01:29:23）。
- **出处**：逐字稿 01:03:32—01:04:00 明确核实；Word `输出两种` 与 Fig. 5 图注的"轮廓。"；你的笔记。
- **执行边界（重要）**：项目原生计算的是连续异常分数图及图像级分数。预测轮廓需要从分数图做阈值化等后处理，必须如实标为可视化后处理，并在图注或方法中交代所用规则。展示用 Otsu 生成的轮廓不等于已经规定了正式部署阈值，更不能用真实标注轮廓代替预测结果。若仍暂缓轮廓方案，应保留真实异常图并明确标注尚未完成，不能用手绘轮廓冒充预测。
- **动作**：下一轮先确定采用哪一种可复现的展示后处理，再制作同一图像的两种输出。

## F05 删除重复图内标题并处理公式归属

- **要求**：删除图像内部重复的 `Fig. N` 大标题和长副标题，保留正文中的正式 caption、必要子图编号与模块名。"删标题"不能执行为删除论文全部图注。上、下分区标题简化，字号与比例统一。
- **出处**：你的笔记、逐字稿 00:53:15—00:53:41（"你放在这里的话，它是个标题"）、01:13:44；Word。
- **★ 补充动作（公式）**：老师指出公式悬放在远处、图内找不到对应变量或运算对象（01:08:42—01:12:13："你突然放个公式上来，你没有任何解释"）。保留的公式应贴近所属模块，并**用虚线（"很虚的那种虚线"）连到对应模块**；解释性长公式可移到正文或副图。图像级分数若出现在图中，也应能追溯到其计算步骤。
- **验收**：图内无重复大标题；图内出现过的每个公式/变量都能在正文找到定义与解释。

---

# 三 变量与字体的统一规范

## N01 先确定正文符号表再改全部图片 ★

- **要求**：以 Problem statement、定义式及最终确认的正文为依据，逐一登记变量的含义、字体、粗细、斜体、大小写及上下标。正文内部若已有冲突，先修正冲突，再同步 PPT。
- **出处**：逐字稿 00:56:39—01:03:15（罗马字体/数学字体、变量强调、图文一致）、Word `变量同意，字体，斜体`、你的笔记。

| 对象 | 统一要求 | 检查重点 |
|---|---|---|
| 普通英文标签 | 与论文正文的英文字体一致 | 正文以 Times New Roman 为主；图内不能继续混用 Arial 等字体（老师原话："图的字体就是正文的字体，也要跟 word 里面的正文字体一样"） |
| 数学变量 | 按同一个定义保持字体与粗斜体 | 不能把标量全部加粗，也不能把向量全部改成普通斜体；老师的强调手段是"斜体、加粗、或换字体" |
| 索引与描述性下标 | 区分可变索引和固定说明文字 | 类别与样本索引按定义；`img` 等说明性下标保持既定正体；`X_c^i` 要明确 i 是 X_c 的下标 |
| 数学算子与数字 | 算子、数值和单位保持规范 | `min`、`Norm` 等按既定正体；数字不因相邻变量而变斜体 |
| 字符本身 | 核对字形与编码 | 字母 l 与 ℓ、R 与 ℝ、减号与连字符不能仅凭相似外形替换 |
| 变量在正文中 | 要在正文里得到强调 | 如 Figure S1/S2 的 `S` 是变量而非单词，应斜体/加粗强调（逐字稿 01:01:17—01:03:15） |

- **★ 字号两条口径要分清**：
  - 01:04:00（**符号统一**语境）："统一的包括字体……这个字号无所谓，主要是看过去长得一样就行"——用于符号统一时确实可以放松；
  - 01:21:16（**图整体观感**语境）："包括这个框的文字……它会小于正文的文字，尽量确保图里面描述任何信息的文字，用肉眼看的时候**大于等于正文文字**就差不多了"——这是主口径，原清单遗漏。
- **执行建议**：普通文本与数学公式分别管理字体。数学公式可沿用正文的 Cambria Math 或最终选定数学字体；不能为追求"全罗马"破坏数学对象的语义和字形。字体家族、正斜体和粗细是三个独立属性，需要分别核对。

## N02 全覆盖检查对象

- **检查范围**：主图、融合图、记忆库图、编码器图、控制实验图、定性结果图、曲线坐标轴、图例、表头、图注与正文中的同一符号。尤其检查参考集合与输入图像、类别与索引、两支特征、融合描述子、类别记忆库、patch 分数、最终异常图、图像级分数和高斯平滑参数。
- **★ 补充动作**：按 Word **标红位置**逐项复核——摘要中的代码地址、3.1 Problem Statement 的变量（c/K/x 附近）、Fig. 1 图注的 `ℓ2`、主结果表（Table 3）与类别表（Table 7）的 Dataset 列、Fig. 4 图注的 `DINO-only`。
- **约束**：不得为了丰富图片添加正文未定义的新变量。确需新符号时，必须同步定义含义、形状及索引范围，并保持代码概念对应。

## N03 老师文件中的字符改动需要逐项判断

- **要求**：老师版本中部分公式文本由 ℓ 变成 l、由数学减号变成普通连字符，还有实数域字形的变化；文件没有修订轨迹，不能断言这些都是老师要求的数学更名。下一轮应按语义和原定义核对，不能把文件里所有字符差异机械照搬。
- **出处**：Word 对比审计（Fig. 1 图注 `ℓ2` 被标红）。

## N04 验收方式

- **动作**：建立一张符号对照表，一列正文定义，一列 PPT 出现位置，一列样式。先自动检查重复文本与字体，再在最终论文版宽和放大视图下人工检查；特别检查上下标没有错位、缺字或被转成图片中的近似字符。
- **验收**：正文和图注的最终版本与导出的图一起验收。

---

# 四 副图与方法章节的修改

## F06 图二补足核心设计细节

- **要求**：图二应说明核心设计而不是重复主图框架：展示两支原始网格、对齐后的对应位置、各支描述子归一化、缩放与拼接、联合归一化和输出维度。补充信息必须对应正文定义。
- **出处**：你的笔记"图二太空，可以补充一些变量名"、Word `Figure 2 describes the core`、逐字稿 01:13:27—01:15:56。
- **执行建议**：用少量对应 patch 的高亮连接说明空间对齐；在输入、对齐后和融合后标注真实变量与张量尺寸；把归一化和拼接用有区别的背景色表达，并补明确的 Fusion 模块名。
- **★ 补充动作**：老师说这张图"单调""框当中要饱满"——**每个运算框/拼接步骤都要有模块名或变量名**（"操作得有个题目""他做了一个 concat，你也得有个名字框架放在上面""你补充上名字，它是个变量"）。
- **约束**：不要把几何网格对齐画成学到的语义匹配。

## F07 图三说明记忆库与异常评分

- **要求（执行建议）**：展示每张正常支持图的 patch 如何汇入类别库、库如何固定，以及一个查询 patch 如何在同一联合空间中选取邻居。标清精确近邻距离、分数恢复网格、插值和平滑与输出之间的关系。
- **约束**：若展示两分支距离的分解，需体现二者评价同一个候选支持 patch，不能画成各自找不同邻居后随意合并。只保留有帮助的维度、变量和局部示意，正文承担完整推导；图中公式的系数必须与正文一致（注意二分之一不能被文本提取误判成 12）。

## F08 编码器和控制实验副图

- **要求（执行建议）**：编码器图说明冻结设置、预处理导致的网格差异、最终保留特征及投影维度；不要误画为两个分支 patch 尺寸不同，或画出没有参与推理的文本路径。控制实验图说明相同参考样本、下游评分与后处理，以及各方法在各自描述子空间建库。
- **动作**：这两张图也应增加必要变量、尺寸和对照关系，统一框线、字体和模块配色。老师没有逐一指定每张补图的新增内容，以上是结合现稿的落地建议。

## P06 方法文字必须解释图中核心思路

- **要求**：Overview 中补完整的图二引用，解释核心思路与设计动机；每张图在正文中先引出，再解释它提供了什么证据。图二展示方法机制，图三展示建库与评分，避免同一段反复叙述主图。
- **出处**：Word 对概览的补充（`Figure 2 describes the core`）、逐字稿 00:41:04—00:42:34 及公式讨论。
- **验收**：图中每个关键模块均能在方法章节找到定义与解释；正文中的输入、输出、变量、冻结条件、记忆库更新规则与图一致。任何扩展分支或前景策略若仅属于未来讨论，应留在规划或局限部分。

## F09 全图排版标准 ★

- **要求**：缩小空框，放大有效文字；边框和箭头更清楚，避免文字细而小；模块内边距、上下行高度和图像大小统一。
- **出处**：你的笔记、逐字稿 01:17:07—01:19:11（"图占了二分之一页……很空，占地面积太大，内容太少了，这个最忌讳了"；"文字内容太小……我宁可把这个字放大"；"整体的字体一样大"）、01:22:35。
- **★ 字号验收口径**：以论文实际嵌入尺寸（通栏约 180 mm）检查，**图内承载信息的文字用肉眼看应大于等于正文**；内部 Fig. 标题删除后重新分配空间，保留能帮助读者理解的子图说明。
- **约束**：不以全屏 PPT 的可读性代替发表尺寸检查。

---

# 五 需要补齐的实验图片

## E01 多方法同样本定性对比图 ★

- **要求**：至少按样品 1 / 样品 2 / 样品 3 组织，列出输入、真实标注（Ground Truth）和各对照方法的结果。**老师原话是"一共有十个方法就十张图了"**，本文方法与 GT 相邻放置，"样品要多一点"。
- **出处**：Word 便条"所有对比方法针对样品1的检测结果，样品2，样品3"、逐字稿 01:29:45—01:32:33。
- **执行建议**：从各算法实际预测文件生成，保留一致的图像裁剪与样本标识；同一样本占同一行，同一方法占同一列，第一张是方法 A、依次到最后是本文方法与 GT 并排。不同方法分数尺度可能不同，要注明热图缩放规则，不能用颜色亮暗直接替代定量比较；若展示阈值化结果，说明阈值协议。样本选择应覆盖不同缺陷与困难场景，不能只保留极端成功例子。
- **验收**：对照方法数量以论文正式对照集合为准（"十个"是老师针对你现有对比集的表述，不是硬性锁定值）。

## E02 本方法五个检测案例与细节放大 ★

- **要求**：至少 **5 个**样本（老师原话："你这才给出四个例子，咱们要不五个吧"），展示整图位置、局部放大和检测结果，能看清细小缺陷或边缘。现有案例不足以满足这一项；原有失败案例应继续保留并清楚解释。
- **出处**：Word 便条"5个本文方法的检测图例子（细节放大版）"、逐字稿 01:33:12—01:35:16。
- **执行建议**：用**红色实线框、不填充**标出待放大区域，小框引出放大窗，另用不同图例标真实标注边界与预测轮廓，避免把"放大框"误认为"算法检测框"。每个样本使用统一的局部窗口与放大倍率标识；图注注明数据集、类别、样本、shot、seed 及案例选择方式。
- **★ 叠加要求**：本方法案例图同样要给"轮廓 + 热图"两种输出（01:28:39—01:29:23）。

## E03 消融实验的可视化 ★

- **要求（修正后的口径）**：老师明确 **DINO-only 不能算消融**（逐字稿 01:33:12：老师问"DINO-only 就是我们的一个消融对吗？"，学生答"不太算吧"，老师说"不能算消融，对不能算消融"）。要补的是"**去掉某一个模块，去掉一个分支的实验，去掉另一个分支的实验**"这类真消融（01:35:16）。
- **动作**：把可视化拆成两类——① **方法/控制组对比**（DINO-only、CLIP-image-only，正文已定义为 matched control，不要改称呼）；② **真消融**（去模块、去分支、去空间对齐、去分支归一化、去 DPAM、拼接检索 vs 分数融合）。消融图只放第②类。
- **★ 先出"缺失消融清单"**：逐项写"缺什么 / 是否已有真实结果 / 下一步是补跑还是直接写入 / 能否写成结论"。已知：论文 §4.1.2 自己列出缺 `alternative spatial alignment`、`branch normalization`、`DPAM`、`feature fusion vs separately searched score fusion`；而 **R1 `MAP_mean`（独立分支 NN 平均）已有真实结果**（同缓存同后处理，K2/K4 相对 A1 +0.005517 / +0.003942，未过 +0.01 门），审计结论是"先整合已有证据，不重复运行"（`docs/project_review_20260910/paper_audit.md`）。
- **验收**：每个声称有效的模块至少有对应证据；参考样本、数据划分与后处理尽量一致；没有实际运行的消融不能出现在图中，未完成的控制组应标记待补。

## E04 曲线图要求需先检查方法适用性

- **要求**：老师要求"所有对比方法 loss 随着 epoch 的变化曲线……如果你不知道找着问，我要，回头艾特我找给你要"（01:36:30—01:37:25）；Word 便条同。
- **执行边界**：当前冻结检索方法没有目标域训练 epoch 或训练 loss，不能为它编造收敛曲线。有训练过程的方法可用真实日志绘制，并注明目标函数及训练数据；不同 loss 定义不能直接凭数值大小比较优劣。对无需训练的方法标注不适用，可考虑用 shot 数、分支数或资源规模与性能的曲线替代——这属于需要向老师说明的替代方案，不是已经批准的替换。
- **待办**：跟催老师发样例图（见第九节）。

## E05 显存与推理速度对比图

- **要求**：老师要求"一个图里面有两个纵轴，一个纵轴是 VRAM 消耗，另一个纵轴是推理速度"，并描述了折线画法：本文方法用红色、对比方法用蓝色，每个方法一条竖线/折线（01:37:25—01:39:31）；Word 便条同。
- **动作**：需要所有可比方法的真实资源数据。记录设备、精度、batch size、输入分辨率、预热、计时同步及重复次数；区分建库成本与单图查询成本。双轴只是表现形式建议，也可用显存—延迟散点图或并列图，但必须标单位并说明速度是 ms/image 还是 images/s。不同硬件的论文公开数字不可当作同机结果。
- **待办**：老师会发一张样例图作参考（"我给你发"，01:38:30）。

---

# 六 表格与实验协议

## E06 表格字号和自己方法整行加粗 + 合并数据集行 ★

- **要求**：统一表头、数据、方法名、均值和标准差的字体与字号（老师原话："你不能一个表格一会这个大太大一会那个太小，你看这个 11 号，有 9.5 是有什么想法吗"，01:23:11—01:23:32；建议全部统一为 9.5 或全部为 11）；表格改为**三线表**（01:23:32）；所有 DCFnet 行统一整行加粗（01:25:45—01:27:14）。
- **★ 合并动作**：同一数据集的多行要**合并单元格/合并为分组**（01:26:35—01:27:00："你要这样放的话是同一个数据集，那你这个合并单元""这样合起来"）。老师说他已经在 Word 里用颜色标出来了（01:27:06："我合并的给你标，我标过颜色"）——对应复核中发现的**主结果表（Table 3）与类别表（Table 7）的 Dataset 列逐格标红**，其中主结果表的 MPDD、BTAD 第二行数据集名已被清空。**标红=合并/分组提示，不是删除数据集。**
- **约束**：表头保持独立层级；如果加粗还用于最优结果，要另设清楚的最优标记规则并在表注说明，避免读者将"本方法加粗"误读为"全部最优"。宽表优先调整布局或拆分，避免继续缩小文字。

## E07 随机种子和统计口径写清楚

- **要求**：逐表、逐图写明 seed 列表、shot 设置、重复次数、支持样本如何抽取，以及均值和标准差跨什么维度计算；**正文要把随机种子的具体数值写出来**（老师原话："随机种子你设置为多少值，在正文当中得描述清楚"，01:28:17）。图注不能只写"多次实验"，也不能把 seed 与不同 K 的配置混作独立重复。
- **执行建议**：当前常见设置为 seeds 0、1、2 和 K 为 1、2、4。若固定 K 后跨三种 seed 汇总，应明确写三次；若表格汇总九个 seed 与 shot 组合，应写九配置汇总并解释其含义。
- **动作**：保存可复现的正常参考样本清单，保证匹配控制使用相同的样本身份；为图中每个案例保留数据集、类别、图像 ID、seed、K、方法版本与预测路径。

## E08 补近期算法并保证比较公平

- **要求**：沿用已整理的对照算法调研文档（`docs/baseline_plan_20260910/`），核查现有结果缺口，逐项补真实实验。区分无需目标训练、目标正常样本适配、源域训练、零样本与少样本设置；不能只按年份把不等价结果放在同一主表。
- **约束**：方法的新颖性评估与对照算法的新近程度是两项任务。引用近期方法并不能证明 DCFnet 首创；论文中要具体解释已有多分支或多尺度方法与本文的机制及实验控制差异。

## E09 用完整结果支撑结论

- **要求（执行建议）**：主表、类别分析、消融、定性案例和资源结果共同支撑结论；像素级、图像级指标的具体名称与计算协议分别写清楚。对无提升或负迁移结果保留解释，不把某项指标的增益写成全面领先。
- **边界**：当前新增分支、阈值校准、收敛曲线及部分对照实验仍是待执行工作，本清单不产生任何新成绩。

---

# 七 修改顺序与完成标准

| 阶段 | 需要交付 | 完成判定 |
|---|---|---|
| 1 语义定稿 | 正式项目名与论文定位说明、贡献草稿、符号表 | 无未实现方法；每个创新点有差异与证据路径；项目名不含 SCI |
| 2 方法图重画 | 可编辑 PPT 及论文插图 + **图源版本清单** ★ | 多图输入、清楚的建库与查询、图二细节、两种输出规则明确；正文图号与图源绑定 |
| 3 实验证据补齐 | 定性对照、五例细节、真消融、资源图 | 使用真实预测与日志；案例和实验协议可追溯；消融图不含 DINO-only |
| 4 论文同步 | 摘要、引言、方法、实验、讨论及图注 | 图文一致；引用和图号完整；结论不过度延伸 |
| 5 投稿排版 | 最新 Word 及最终图表 | 所有 DCFnet 行加粗、同一数据集已合并；字号清楚；变量统一；无课堂占位或内部大标题 |

**★ 阶段 2/5 的版本绑定动作**：建"正文图号 ↔ 图源文件 ↔ PPT 页 ↔ 数据/脚本 ↔ 版本日期"清单。现状是正文引用 Figure 1–6 与 S1–S2，而最新图包 `docs/figures_contour_notation_20260911/` 只有 5 张方法图（Fig1_Main、Fig2_Fusion、Fig3_Memory、FigS1_Encoders、FigS2_Control），实验图 Figure 4/5/6 没有同一版本图源——`docs/project_review_20260910/paper_audit.md` 已把它列为 P0。

---

# 八 下一轮必须逐项核对的清单

- □ 题目与 DCFnet 全称一致；研究目标和实现范围一致。
- □ **项目/仓库名不含 SCI，摘要与 Data and Code 的地址可访问且指向正式命名。** ★
- □ 常规评估不再独占一个核心方法创新点；模块拆分有独立依据。
- □ 引言有明确的 gap 与本文定位；"first" 主张已核查、有文献支撑。
- □ 多分支扩展放在正确章节；Overview 与主图没有出现未实现的分支。
- □ **ROI / 前景方向已登记，与 E7-F 方案书一致；正文没有把它写成已实现；若采用，贡献处已展开并配了设计动机图。** ★
- □ 主图支持端是多张正常图；共享特征变换与固定记忆库关系清楚。
- □ **主图两条任务路径有背景色区分；图内公式用虚线连到所属模块。** ★
- □ 两种输出来自同一样本；轮廓的后处理与真实异常图明确区分；案例图也是两种输出。
- □ 图内重复总标题删除，论文正式 caption 与子图编号保留。
- □ 图二有真实变量、尺寸与核心机制；每个运算框有模块名或变量名；公式紧邻所属模块。
- □ 全文和全部图片的符号、字体、粗斜体及上下标逐项一致。
- □ **图内承载信息的文字在最终论文尺寸下大于等于正文。** ★
- □ 至少五个本方法细节案例（红色不填充框 + 放大窗）；有多方法同样本对比（≈10 方法 + GT，本文与 GT 相邻）和模块证据。
- □ **消融图只含"去模块/去分支"类真消融（DINO-only 归为控制组）；已出"缺失消融清单"；R1 MAP_mean 结果已并入正文/补充材料而非重跑。** ★
- □ loss 曲线适用性已说明；显存与速度来自可比实测。
- □ 表格可读，DCFnet 整行加粗，**同一数据集多行已合并/分组**，最优标记规则不混淆。
- □ seed、K、重复次数、汇总维度及样本清单写清楚；随机种子具体数值已写入正文。
- □ **正文图号、图源文件、PPT 页、数据脚本已绑定为同一版本。** ★
- □ 最新图片已替换进最终 Word，而不仅保存在 PPT 或 PDF。

---

# 九 需要向老师确认或索取

1. 项目名称与最终研究定位的措辞（**并明确是否要把 GitHub 仓库名一起改掉、旧地址如何处理**）。
2. 拆分后的模块是否有足够独立新意。
3. 轮廓展示采用的后处理协议，以及是否进入正式方法。
4. 冻结方法无法提供 loss-epoch 曲线时，老师接受的替代图。
5. **老师答应要发的内容**：VRAM/速度样例图、高质量主图参考图（逐字稿 01:38:30、01:40:02；Word 便条"问我要"）——**需主动跟催**。
6. ROI / 前景方向是本轮就做还是先留作候选；Word 里 `dyt layer` 具体指什么（逐字稿只有模糊读音，老师未解释）。
7. 图内文字"大于等于正文"是否适用于所有图，还是只要求承载信息的文字（老师原话是"描述任何信息的文字"）。
8. 多方法对比图的对照集合最终取哪些（逐字稿 01:30:46 的"十个方法"指你现有的对比集，仍以论文正式对照集合为准）。
9. **若采用主体/前景模块，贡献处那张"设计动机图"由你自己画还是老师提供样例。** ★

---

# 十 老师原件来源对照

| 来源 | 内容 | 用途 |
|---|---|---|
| `DCFnet_English_Polished_20260906(2).docx` | 红字 73 处 / 绿字 28 处；与 0906 稿 diff 出 8 组文字改动（贡献列表 4 处、3.2 Overview、`变量同意，字体，斜体，输出两种`、Fig. 5 图注"轮廓。"、5 条中文任务便条）；无批注、无修订记录、无高亮、无文本框 | 书面要求第一手依据 |
| Word 标红但未改文字的位置 | 摘要代码地址、3.1 Problem Statement 变量附近、Fig. 1 图注 `ℓ2`、主结果表（Table 3）Dataset 列（MPDD/BTAD 第二行名已清空）、类别表（Table 7）Dataset 列、Fig. 4 图注 `DINO-only` | 逐项复核对象 |
| 课堂逐字稿 | 396 段 / 3.4 万字 / `00:06:36—01:40:25`，含时间戳与说话人 | 口头要求第一手依据 |
| 腾讯会议页面 AI 纪要 | 6 章纪要 | 已与逐字稿逐条比对，结论一致，无额外要求 |

**逐字稿关键时间点**

| 时间 | 内容 |
|---|---|
| 00:07:40 | 期刊要求提交 Word；最新图文要进 Word 正文 |
| 00:09:28 | 项目/仓库名不得写 SCI，改为与论文名相关的命名 |
| 00:10:34—00:12:43 | 常规实验评估不能作为 contribution |
| 00:12:54—00:15:19 | 第一条 = novel framework；第二/三条 = 模块及其差异 |
| 00:21:40—00:26:49 | 参考 ROI / 前景聚焦机制作为候选创新点（含降背景计算成本的动机） |
| 00:31:48—00:36:48 | 多分支融合边界思路被认可；贡献可拆但每点要说清不同 |
| 00:40:00—00:41:33 | 若采用前景模块，贡献处展开 + 配设计动机图 |
| 00:43:38—00:45:38 | 三分支对比放实验分析，不进 Overview |
| 00:49:20—00:51:03 | 引言/贡献要写明确的空白与本文定位，措辞可强但需自洽 |
| 00:53:15—00:55:46 | 删图内标题；主图要达到图文摘要质量 |
| 00:56:39—01:03:15 | 公式/变量字体、斜体加粗、图文一致 |
| 01:03:32—01:04:00 | 输出热图 + 轮廓两种；符号统一时字号可放松 |
| 01:06:17—01:07:06 | 两个任务用两种背景色区分 |
| 01:08:42—01:12:13 | 公式要有解释、紧邻模块、用虚线连过去 |
| 01:13:27—01:15:56 | 图二的框要饱满，每个操作框要有模块名或变量名 |
| 01:17:07—01:19:11 | 图占地过大、文字过小；全图字号统一 |
| 01:21:16 | 图内承载信息的文字要大于等于正文 |
| 01:23:11—01:27:14 | 表内字号统一、三线表、本方法整行加粗、同一数据集合并（已用颜色标出） |
| 01:28:17 | 随机种子具体数值要写进正文 |
| 01:28:39—01:29:23 | 实验图也要给热图 + 轮廓两种输出 |
| 01:29:45—01:32:33 | 多方法同样本对比图（≈10 方法 + GT，本文与 GT 相邻，样品要多一点） |
| 01:33:12—01:35:16 | DINO-only 不能算消融；补 5 个红色不填充框的细节放大案例 |
| 01:36:30—01:37:25 | 补 loss-epoch 曲线；老师会发样例 |
| 01:37:25—01:39:31 | 补双纵轴 VRAM vs 推理速度图；老师会发样例 |

## 复核边界

- 老师 Word 无批注与修订记录，颜色是唯一标注手段；`变量同意，字体，斜体，输出两种` 这段未着色，位置在 3.2 之后，理解为对方法图/变量的整体要求。
- 3.1 段标红 run 的文本为空（变量以对象/公式形式存在），需在 Word 中人工确认标红的具体对象。
- 逐字稿为腾讯会议自动转写、未做人工逐句校对：专名和数字仍可能有误（例："S ci"、"光速"=ground truth、"阿布"=output、"飞车"=fusion）。凡引用原话处均已标注时间点，正式写入论文前请回看对应片段。
- 本文件只做要求整理与现状核对，未修改论文、PPT、图源，也未运行任何新实验。
