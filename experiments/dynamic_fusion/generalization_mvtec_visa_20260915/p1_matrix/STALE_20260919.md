# 过期索引说明（2026-09-19 只读核查）

本文件仅作**只读核查记录**，不修改同目录下任何实验记录/工具产物。

## 1. 复核结论：两项「疑似过期」经核对已是最新

| 文件 | 任务来源所述 | 2026-09-19 磁盘实际 | 处置 |
|---|---|---|---|
| `STATUS.json` | `remaining_seconds: 80951`、`completed: 225` | `state=completed`、`completed_units=324`、`expected_units=324`、`updated_utc=2026-09-17T17:12` | **无需处理**（已是最新）；全仓检索不存在 `remaining_seconds` 字段 |
| `RUN_SUMMARY.json` | 陈旧 | `state=completed`、`expected_units=324`、`completed_units=324`、`missing_units=[]` | **无需处理**（已是最新） |

## 2. 内部自相矛盾的记录（未擅自改写）

| 文件 | 过期/矛盾字段 | 现值 / 现状 | 判定依据 | 建议 |
|---|---|---|---|---|
| `DEVICE_DEVIATION.json` | `scope` 写 “the remaining VisA units … (99 of 144 at the time of the switch)”，而 `consequence_for_the_artefact.statement` 写 “the first 55 units were produced with device=cpu” —— 99 与 55 互相矛盾 | 实测 **VisA cpu=55、cuda=89** | 对 `p1_matrix/units/**/DONE.json`（共 324 个）的 `engine.device` 逐条统计：`visa_s0_k1..k8` 各 12 = 48 cpu；`visa_s1_k1` = 7 cpu + 5 cuda；`visa_s1_k2/k4/k8` = 36 cuda；`visa_s2_k1..k8` = 48 cuda。故 “first 55 units cpu” **正确**；“99 of 144 at the time of the switch” 与「已完成 55 / 剩余 89」不符（应为 89，或需注明 99 的另一种口径） | 该文件**未被任何脚本读取**（仅被文档引用）；但它是实验证据记录，含唯一的历史口径说明，**故未就地改写**，改以本说明记录裁定依据。**建议由作者改判据**（把 99 更正为 89，或补充说明 99 的统计时点/口径） |

## 说明

- `_gpu_probe/` 下的 9 个探针子目录已于 2026-09-19 清理；`_gpu_probe/DEVICE_PARITY.json` 与同目录 `*.log` 保留。注意 `DEVICE_PARITY.json` 的 `source_outputs.a/b` 仍指向已删除的 `_gpu_probe/cpu|...`、`_gpu_probe/cuda|...` 路径（仅路径失效，统计数值仍在文件内）；如需复现，重跑 `scripts/limitation_closure_20260915/probe_device_parity.py`。
