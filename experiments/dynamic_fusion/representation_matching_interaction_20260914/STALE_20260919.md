# 过期索引说明（2026-09-19 只读核查）

本文件仅作**只读核查记录**，不修改同目录下任何被脚本读写或有唯一证据价值的文件。
下列文件**被工具脚本读写**，因此按纪律改为「新增本说明」而非就地改写；正式刷新应通过**重跑生成脚本**完成。

## 1. 被脚本读取或写入（故不改写，仅记录）

| 文件 | 过期字段 | 现值 / 现状 | 判定依据（谁读它） | 建议 |
|---|---|---|---|---|
| `ARTIFACT_MANIFEST.json` | `created_utc=2026-09-14T13:22:43`；约 50 条 `bytes`/`sha256`；未覆盖 2026-09-15 起的新产物（如 `05_baselines_multi_dataset/*`、`05_extra_encoders/wide_scope/*`、09-19 重渲染的图表） | 清单本身仍为 09-14 快照；同目录多份表已被重算（`05_baselines/resource_comparison_v2.csv`、`baseline_common_frame.csv` 均已留 `.bak_20260919`） | `scripts/representation_matching_interaction_20260914/finalize_new.py`（268–292 行生成/写入）；`selfcheck.py`（74 行）将其列为「模板文件」 | **重生**：重跑 `finalize_new.py`（或在新收口流程中重生清单）。不要手改 sha。 |
| `RUN_SUMMARY.json` | `created_utc=2026-09-14`；`stages` 仅到 `S9_resource_measurement`；`artefact_count=829`、`artefact_bytes=8015572367`；`selfcheck = 71/71` | 现状：`SELFCHECK.json` 记 `checks=71, passed=69`（2 项失败见其 `failed`）；且 09-15 起新增阶段（多种子扩展、四数据集泛化、KSDD2 确认）未登记 | `scripts/representation_matching_interaction_20260914/finalize_new.py`（269、278、290 行：读 `SELFCHECK.json`/`READONLY_PROOF.json` 后写本文件） | **重生**：重跑 `finalize_new.py`；`selfcheck` 字段应取当次 `SELFCHECK.json`。 |
| `05_baselines/S4_SUMMARY.json` | `coverage` 为**二数据集**口径：`target_conditions=72`、`baseline_rows_matched=72`、`common_frame_rows=124` | 四数据集口径在 `05_baselines_multi_dataset/S8_SUMMARY.json`（`units_expected=144`、`units_completed=144`；四数据集共同区域表 864 行） | `scripts/representation_matching_interaction_20260914/s5_paper_assets.py`（72 行）、`finalize_new.py`（90、151 行）**读取**该文件 | **改判据或重生**：引用四数据集 `S8_SUMMARY.json`；不要手改 `S4_SUMMARY.json`（会被脚本读回）。 |

## 2. 说明

- 本目录另有 `STATUS.json`、`FAILURES.json`、`READONLY_PROOF.json` 等工具产物，本次未改动。
- 上述三项的「现值」以 2026-09-19 磁盘状态为准；如后续重跑生成脚本，请以新生成的文件为准并删除本说明。
