# 下一步（收口包之后）

1. 作者判断：主表是否改用管线忠实掩码（BTAD-03）重算；若改，按 1.7 的成本执行，并把 `04_recheck/btad03_mask_variants.csv` 换成正式表。
2. 论文若需要“全像素统计显著”，需在冻结范围内单独补全像素配对区间。
3. 文献差异表完成页码级核对（当前 4 篇为摘要/结构级）。
4. 若主张更广的随机支持可靠性，需先冻结新增 seed 与停止标准，不得因边界结果临时加复制。
