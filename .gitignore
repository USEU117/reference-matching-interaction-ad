# Datasets and downloads
data/downloads/
data/mvtec_ad/
data/mvtec/
data/mvtec_patchcore_fewshot*/
data/visa_raw/
data/visa/
data/visa_1cls/
data/visa_pytorch/
data/visa_patchcore*/
data/visa_candle_smoke/
data/btad_raw/
data/mpdd_raw/

# Third-party source mirrors
methods/

# Environments
.venv*/
venv/

# Experiment outputs
outputs/
*.log

# Model weights (repo policy: checkpoints stay local / under git-ignored outputs)
*.pt

# Local scratch / QA renders (not release artifacts)
/.tmp_*/
.qa_render_*/

# Staged dataset copies in experiments (do not redistribute)
experiments/**/staged_*/
experiments/**/predictions/

# Large binary prediction caches
*.npz
*.npy
# ...but the compact package's replayable per-image maps ARE release artifacts (track them)
!submission_repro_20260827/predictions_compact/maps/**/*.npz
# ...and the compact package's feature-export logs are part of the evidence chain (track them)
!submission_repro_20260827/logs/**/*.log

# Local PatchCore baseline caches (referenced by scripts and by baseline_common_region.csv,
# so they stay on disk; they are machine-local artifacts and are not redistributed)
data/patchcore_closeout/
data/btad_patchcore_mvteclayout/

# Office lock/owner files
~$*

# DG-SAFE (innovation_v6) scratch audit re-runs (frozen audit dir stays canonical)
experiments/dynamic_fusion/innovation_v6_dgsafe/export_out_*/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# IDE / OS
.idea/
.vscode/
.vs/
.trae/
.DS_Store
Thumbs.db

# Raw KolektorSDD2 data (script data root; machine-local, ~850 MB, do not redistribute)
data/kolektorsdd2_raw/

# Replication package build output (published separately, not tracked)
dist/

# Build backups from the manuscript rebuilds (regenerable, hundreds of MB)
*.bak*

# Manuscript binaries and figure packages: rebuild from source, do not version (each rebuild is ~27 MB)
*.docx
docs/figures_package_*.zip
# ...but the single current authoritative manuscript docx is a release artifact (track this one file only)
!docs/paper_complete_review_20260920/Reference_Matching_Complete_English_20260923.docx

# The polished layout template is an INPUT (binary, not rebuildable from source) - keep it tracked
!docs/manuscript_polished_20260919/*.docx
